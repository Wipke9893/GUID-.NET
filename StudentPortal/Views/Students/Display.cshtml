@model StudentViewModel

<h1>Display Student</h1>

<div>
    <h2>@Model.Name</h2>
    <div><strong>Email:</strong> @Model.Email</div>
    <div><strong>Phone:</strong> @Model.Phone</div>
    <div><strong>Subscribed:</strong> @Model.Subscribed</div>
</div>
